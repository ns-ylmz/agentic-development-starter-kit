# Security Model

<!-- Fill in when authentication/authorization is designed. -->

## Authentication

<!-- Token strategy, session lifecycle, refresh behavior. -->

## Authorization

<!-- Roles/permissions model, where checks are enforced. -->

## Secrets & Configuration

<!-- What is secret, where it lives, what must never be committed (guard-git.sh blocks staged .env/key files). -->
